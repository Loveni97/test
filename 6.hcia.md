常用命令

dis port vlan 查看端口vlan
dis vlan 查看vlan
display ip interface brief       // 查看三层接口UP/DOWN

port trunk pvid vlan 1 修改trunk默认vlan

vlan batch 10 11
vlan batch 10 to 20

display mac-address              // 查看MAC表
display arp                      // 查看ARP表
display interface GigabitEthernet 0/0/1  // 看单个口状态

shutdown               // 关闭接口
undo shutdown          // 开启接口
display interface brief  // 查看所有接口状态




1.交换机下两个vlan配置

vlan 10
vlan 20


int g0/0/1
p l a
p d v 10


int g0/0/2
p l a
p d v 20


int vlanif 10
ip add 192.168.10.1 24

int vlanif 20
ip add 192.168.20.1 24


2.路由器下单臂路由

int g0/0/0.1
dot1q termination vid 10
ip add 192.168.30.1 24
arp broadcast enable

int g0/0/0.2
dot1q terminiation vid 20
ip add 192.168.40.1 24
arp broadcast enable

交换机

vlan 30
vlan 40


int g0/0/1
p l t
port trunk allow-pass vlan all

int g0/0/2
p l a
p  d v 30


int g0/0/3

p l a
p d v 40

3.在路由器配置dhcp

dhcp enable

ip pool vlan10
gateway-list 192.168.10.1
network 192.168.10.0 mask 255.255.255.0
dns-list 



int g0/0/0.1
dhcp select global


 4. 负载均衡和冗余





5.ACL

# 新建高级ACL
acl number 3000
# 允许 10 → 20 的 ping
rule permit icmp source 192.168.10.0 0.0.0.255 destination 192.168.20.0 0.0.0.255

# 但可以让20 → 10 的 ping应答
rule permit icmp source 192.168.10.0 0.0.0.255 destination 192.168.20.0 0.0.0.255 icmp-type  echo-reply

# 禁止 20 → 10 的 ping
rule deny icmp source 192.168.20.0 0.0.0.255 destination 192.168.10.0 0.0.0.255
# 放行其他所有流量（DHCP/DNS/上网）
rule permit ip

# 在两个子接口都调用
interface GigabitEthernet 0/0/0.10
 traffic-filter inbound acl 3000

interface GigabitEthernet 0/0/0.20
 traffic-filter inbound acl 3000

dis acl 3000 //查看配置，undo rule 序号，删除规则，rule 序号，插入规则 



6.nat

interface GigabitEthernet0/0/2
 ip address 10.1.1.1 255.255.255.0

interface GigabitEthernet0/0/2
 nat outbound 2000



acl number 2000
 rule permit source 192.168.10.0 0.0.0.255
 rule permit source 192.168.20.0 0.0.0.255


7.关于rip

rip 1
ver 2
network 10.0.0.0 

rip split-horizon 默认open
rip poison-reverse 默认close 

undo rip input  禁止接收rip包
undo rip output 禁止发送rip包
silent-interface g0/0/0 被动接口，只收不发 优先级高于上面两个


dis rip 1 int g0/0/0
dis rip 1 int g0/0/0 verbose 详细信息
dis rip 1

int g0/0/0
rip metricout 2 发出去固定多少值
rip metricin 2 收到在原有基础上+2


acl 2000
rule 1 permit so 6.6.6.6 0

filter-policy 2000 import g0/0/0


8.ospf 

1是icmp  6是tcp  17是udp 89是ospf 

router-id 可以手动配置，其次看活动回环口，ip最大，最后看活动物理口，IP中最大，非抢占性



ospf timer hello 9 修改hello时间，deadtime 默认四倍


router-id唯一
hello包和dead时间一致
区域id一致
认证一致
子网掩码一致
网络地址一致
mtu一致


缺省路由发布
ospf 1
default-route-advertise


认证
接口认证
区域认证
接口认证优先


int g0/0/0
ospf authentication-mode md5 1 cipher wakin

ospf 1
area 0
authentication-mode md5 1 cipher wakin


GVRP
接口开启gvrp

单向传播
接口需要 p l t p l a v all 


# 9.STP

组播
01-80-C2-00-00-00

BPDU 两种类型 Configuration 和TCN拓扑改变通知


桥ID
2字节优先级，6字节桥MAC

优先级默认32768

端口ID
高8位优先级，缺省值128
低8位编号


根端口选择：
1.到根桥cost
2.对端桥ID
3.对端PID
4.本端PID


指定端口规则:
1.每条链路上，发出开销，BID，PID依次比较
2.根桥上的端口都是指定端口（除了一种情况：自环）


最大阻塞时间20s(如果没收到BPDU20s，则不阻塞)，然后15s学习，再15s转发
message age每经过一个交换机+1
发送hello两秒

## 改优先级
stp priority 4096  步长4096
stp root primary/secondary  相当于0/4096

stp cost 10 改开销
stp cost priority 144 优先级，步长16

stp port priority 



# DHCP
属于应用层协议，基于UDP传输并使用67(服务器)/68(客户端)端口

在三层交换机情况下

如果已经通过int vlanif 10 配置ip 网关下
dhcp enable
dhcp select interface
dhcp server dns-list 8.8.8.8


dis ip pool int vlanif10 all
dis ip pool int vlanif10 used

## 1.四种数据包

discover

offer

request

ack 服务器对req报文确认

nak 服务器对req报文拒绝

release

命令
ipconfig /release


租期一半，申请续约租期一半
还有一个期限7/8


win如果申请不到地址，则自行分配169.254.0.0/16,后两位随机，相同处境的主机可以临时通讯


## 2.地址池

接口地址池（优先）
全局地址池
dhcp enable
    dhcp select interface
    dhcp server dns-list 
    dhcp server lease 
    dhcp server excluded-ip-address 
    dhcp server static-bind ip-address x.x.x.x mac-address xxxx-xxxx-xxxx



ip pool pool1
network 192.168.10.0 24
gateway-list 192.168.10.1
dns-list 8.8.8.8 114.114.114.114


int g0/0/0
dhcp select global

# ACL

acl 数字

2000-2999  基本acl，只能匹配源地址 # acl permit/deny source 1.1.1.1
3000-3999  高级acl，源IP 目标IP 源端口 目标端口 # acl 协议 permit/deny source 1.1.1.1 0 source-port eq 123 destination 2.2.2.2 0 destination-port eq 123
4000-4999  L2层，mac地址
5000-5999

int g0/0/1
traffic-policy inbound/outbound acl 3000

匹配网络中所有奇数地址
192.168.10.1 0.0.0.254
匹配网络中所有偶数地址
192.168.10.0 0.0.0.254 

dis traffic-policy applied-record #查看策略应用情况


配置原则

1.对于基本acl，配置在距离目标的最近出站方向
2.对于高级acl，配置在距离源头的最近入站方向


# NAT

静态NAT

int g0/0/0
nat static en
nat static global 10.0.0.2 inside 192.168.0.2

动态NAT

nat address-group 1 12.0.0.50 12.0.0.60

acl 2000
rule permit so 192.168.0.0 0.0.0.255

绑定acl和地址池
nat outbound 2000 address-group 1 no-pat



NAPT或PAT技术


多个内部地址映射到同一个出口地址的不同端口

nat outbound 2000 address-group 1



easyIP技术

适用于小规模局域网中主机访问外网，直接关联接口，因为它们的地址是通过拨号获得临时IP


nat outbound acl 2000 #只关联acl



## NAT server

将内网服务端口提供给外网使用

比如：
nat server pro tcp/udp  global 外网接口地址(如果是当前接口，那么就是current-interface；也可以是网段中其他地址) 端口 inside 192.168.0.100 80

nat server protocol tcp global 12.0.0.56 2222 inside 192.168.0.100 80


外网访问内网的两种方法：
1.NAT
2.vpn:隧道+加密

# PPP

面向串口

HDLC和PPP

int s4/0/0
link pro ppp/hdlc 修改接口的链接协议





int lo 1
ip add 12.0.0.2 24

int s4/0/0 
ip add unnumbered int lo 1 租用回环口地址

ip route-s 12.0.0.1 24 s4/0/0 指定回包出接口


重点PPP

认证模式：PAP CHAP

密码认证协议PAP 明文，二次握手，可以无限尝试（所以可以暴力破解），发送方为被认证方

CHAP 使用md5加密，三次握手，首先认证方发送，被认证方回复，然后认证方确认


aaa
local-user wakin password cipher 123.com
local-user wakin service-type ppp


举例：
1.pap模式
认证方
interface Serial4/0/0
 link-protocol ppp
 ppp authentication-mode pap

被认证方
interface Serial4/0/0
 link-protocol ppp
 ppp pap local-user wakin password cipher 123.com
 ip address unnumbered interface LoopBack1

2.chap模式

interface Serial4/0/0
 link-protocol ppp
 ppp chap user wakin 
 ppp chap password cipher 123.com

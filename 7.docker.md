基础命令
docker version 显示 Docker 版本信息
docker info 显示系统信息，如容器数、镜像数、存储驱动等
docker help 显示 Docker 命令的帮助信息
docker stats实时查看容器的使用率统计信息
docker events实时订阅Docker 服务器事件

镜像操作
docker pull [image]从远程仓库拉取指定镜像到本地
docker images列出本地所有镜像
docker build-t [tag].从当前目录构建镜像，并指定标签
docker search [image] 在 Docker Hub 上搜索镜像
docker push [image]将镜像推送到远程仓库
docker rmi [image]删除本地镜像
docker tag [image][new_image]为现有镜像创建新的标签
docker history [image]查看镜像的创建历史
docker inspect [image]查看镜像的详细信息
docker save -o [fle.tar] [image]将镜像保存为tar 文件
docker load-i [file.tar]从tar 文件中加载镜像

容器操作
docker run [options][image]以指定镜像创建并动容器
docker create [options][image]创建一个容器但不启动，便于后续启动或配置
docker ps列出当前正在运行的容器
docker ps-a列出所有容器，包括已停止的容器
docker stop [container]停止正在运行中的容器
docker start [container]启动一个已停止的容器
docker restart [container]重启容器
docker rm[container]删除容器(只能删除已停止的容器)
docker exec [container][command]在运行中的容器中执行命令
docker logs [container]查看容器的日志输出
docker logs -f
docker logs -f --tail 100  最后实时100行
docker logs --since "2026-05-10T10:00:10" [container]
docker kill [container]强制停止容器
docker attach[container]附加到正在运行中的容器上
docker commit [container] [new_image]将容器当前状态保存为新的镜像
docker export [container]>[fle].tar 导出容器文件系统
docker import [file].tar [image]从 tar 文件导入为镜像
docker rename [container] [new_name]重命名容器
docker pause [container]暂停容器内的所有进程
docker unpause [container]恢复被停的容器进程
docker cp [container]:/path [host_path]在容器和主机间复制文件
docker top [container]查看容器内正在运行的进程
docker diff[container]查看容器文件系统相对于镜像的更改


数据卷管理
docker volume create [name]创建一个新的数据卷，用于持久化存储
docker volume ls 列出所有数据卷
docker volume inspect [name]查看指定数据卷的详细信息
docker volume rm [name]删除数据卷
docker volume prune 清理未使用的数据卷，释放空间


网络管理
docker network create [network]创建自定义网络，用于容器通信
docker network ls 列出当前可用的Docker 网络
docker network inspect [network]查看指定网络的详细信息
docker network connect [network] [container]将容器连接到指定网络
docker network disconnect [network] [container]将容器从网络中断开
docker network rm [network]删除网络
docker port [container]显示容器端口映射关系

Docker Compose
docker-compose up 启动 Docker Compose 定义的多个服务容器
docker-compose down 停止并移除 
Docker Compose 启动的容器、网络、卷等
docker-compose build 构建或重新构建服务镜像
docker-compose logs 查看 Compose服务容器的日志输出
docker-compose ps 查看Compose 管理的容器状态
docker-compose exec在运行中的服务容器内执行命令
docker-compose start 启动已存在的服务容器(不构建镜像)
docker-compose stop 停止已启动的服务容器
docker-compose restart 重启服务容器
docker-compose rm 删除已停止的服务容器
docker-compose config验证并查看Compose 文件的配置内容

其他高级命令
docker system prune清理无用数据(停止容器、悬空镜像/卷/网络)
docker system df 查看 Docker 使用的磁盘空间情况
docker login 登录 Docker Registry( Docker Hub)
docker logout 登出当前的 Docker Registry
docker update [container]更新容器资源限制，如内存、CPU配额等



docker exec -it 77f mysql -uroot -p123456 -e "show databases;" 命令行进入容器

docker exec -it 容器id bash 进入容器

---
- name: play1
  hosts: datacenter1
  gather_facts: no
  tasks:
    - name: import tasks
      import_tasks: tasks/web_task.yml
    - name: echo group var
      debug:
        msg: "{{ datacenter1_group_var }}------{{ datacenter1_group_service }}"
    - name: echo host var
      debug:
        msg: "{{ centos_host_var }}"

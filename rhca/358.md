
19.

wget  webserver.conf.j2
wget  index.html.j2

vim nginx.yml

---
- name: config nginx server

 





20.






21.


ansible all -m shell -a "systemctl stop nginx"
student@workstation   lab printing-config start

echo -e "servera\nserverd" > inventory

vim print.yml

---
- name: print
  hosts: servera,serverd
  tasks:
    - name: install
      yum:
        name: cups,avahi
        state: present
    - name: start services
      service:
        name: "{{ item }}"
        state: started
        enabled: yes
      loop:
        - avahi-daemon
        - cups
    - name: open firewalld
      firewalld:
        service: mdns
        state: enabled
        permanent: yes
        immediate: yes
    - name: create print queue
      command: lpadmin -p my-printer -v ipp://serverc.lab.example.com:631/printers/rht-printer -m everywhere -E
    - name: make default printer
      command: lpadmin -d my-printer

ansible-playbook printing.yml
